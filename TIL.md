# Python 기초과제 1 — 도서관 관리 시스템

## TIL

### Date: 2026-08-19

- 이날은 과제 요구 조건을 처음 제대로 보았다. 문제를 파악하고 어떤 구조로 가야 하는지 그림을 그리는 게 어려웠다.
- 우선 필요한 환경을 먼저 세팅했다.
- 그래서 ChatGPT에 어떤 것부터 하는 게 좋을지 물어봤고, 프로젝트의 기본적인 흐름을 공부했다.

#### 프로젝트 개발 흐름

```text
(1) 환경 구성
[uv · 가상환경(.venv) · VS Code]
        ↓
(2) 데이터 구조 설계
[List · Dictionary · 자료형]
        ↓
(3) 클래스 설계
[OOP · Class · 상속 · Getter/Setter]
        ↓
(4) 샘플 데이터 준비 - (선택적으로 추가)
[List + Dictionary]
        ↓
(5) 객체 생성 및 테스트
[객체(Instance) · Method]
        ↓
(6) 유틸리티 / 예외 처리
[함수 · try/except · ValueError]
        ↓
(7) CLI 메뉴 구현
[input · while · if/elif]
        ↓
(8) 기능 연결
[등록 · 조회 · 검색 · 대여/반납]
        ↓
(9) 예외 상황 테스트
[입력 검증 · 중복 ISBN · Debugging]
        ↓
(10) 최종 정리
[Module · Package · import]
```


### Date: 2026-08-21

- 오늘도 여전히 시작하려니 막막했다. 그래서 우선 샘플 데이터를 만들고, 부모 클래스와 자식 클래스에 들어갈 정보를 주석으로 정리했다. 그리고 키-값을 어떻게 넣을지를 고민했다.
- 샘플 데이터를 처음에 `models`에 넣었다가 패키지라는 걸 생각하니 `data` 폴더를 새로 생성해 관리하는 게 맞다는 판단을 했다.
- 부모 클래스를 먼저 작성했다. `__init__`에는 주요 변수들을 정의했다.
- 아래는 Getter를 이용해 정보를 불러오는 함수를 정의하고, `is_borrowed`라는 대여 여부를 보여주는 변수에 대해서만 Setter를 이용해 참일 때 '대여중'이라는 조건을 설정해주었다.
- 다음 자식 클래스에는 부모 클래스를 import해온 후 아래에 각각 `EBook`과 `PaperBook`을 각각 클래스로 정의하였다.
- 모든 클래스에는 정보 조회를 위한 `display_info()` 함수가 정의되어 있다. 여기서도 `self` 키워드로 매개변수를 받았기 때문에 문자열 포맷팅할 때 중괄호 안에 `self.__title` 이런 식으로 `self`로 받아주어야 한다는 걸 다시 배웠다. (그 전에는 `get_title`, `get_title()`, `self.title` 등으로 넣었어서 계속 오류가 났다.)
- 현재 시각 오후 8시 48분. 내일은 많이 못하니까 모레 일요일날 완성하자!! 컨디션 관리도 잘 하자 ...!


### Date: 2026-08-23

- 집에서 Mac OS 환경으로 `git clone <레포주소>`를 사용해 기존 프로젝트를 가져와 이어서 작업하였다. `git clone`은 정상적으로 완료되었으나 `uv sync` 실행 시 `src/<프로젝트파일명>/__init__.py`를 찾을 수 없다는 오류가 발생했다. Windows에서 작업할 때 해당 폴더와 `__init__.py`를 임의로 삭제했던 것이 원인 중 하나로 보였다. **(해결중)**

- 필요한 폴더를 다시 생성한 뒤 `uv sync`를 실행했지만 문제가 계속되어 튜터님께 문의하였다. 이후 가상환경과 설치 패키지를 다시 구성하면서 실행 환경을 정리하였다. 패키지 구조와 관련된 부분은 추가 확인이 필요하다. **(해결중)**

- `main.py`에 `while True`를 이용한 기본 이벤트 루프를 만들고, 도서 등록 / 전체 도서 조회 / 도서 검색 / 도서 대여·반납 / 종료의 CLI 메뉴를 구성하였다. 반복해서 사용할 화면 출력 및 입력 검증 기능은 `helper.py`의 함수로 분리하였다. **(해결)**

- 메뉴에 문자를 입력하면 `int()` 변환 과정에서 `ValueError`가 발생해 프로그램이 종료되는 문제가 있었다. `helper.py`에 `validate_input_value()`를 만들고 `try/except`를 이용해 잘못된 입력을 처리하도록 수정하였다. **(해결)**
  - `if/else`를 이용한 조건 처리와 `try/except`를 이용한 예외 처리의 차이를 실제 오류를 통해 확인하였다.

- `main.py`와 `validate_input_value()` 양쪽에서 `input()`을 호출하면서 메뉴 번호를 여러 번 입력해야 하는 문제가 발생했다. 입력은 `validate_input_value()`에서 한 번만 받고, 검증된 값을 `return`하여 `main.py`의 `choice`에 저장하도록 수정하였다. **(해결)**
  - 함수가 `return`한 값을 `choice = 함수()` 형태로 받아 다른 코드에서 활용하는 흐름을 복습하였다.

- 샘플 데이터 `book_info`는 `List[Dictionary]` 형태인데, 처음에는 이 데이터를 그대로 수정하면서 프로그램을 구현하려고 했다. 하지만 `PaperBook`, `EBook` 클래스에서 정의한 메서드를 실제로 활용하려면 샘플 데이터를 객체로 생성하여 관리해야 한다는 점을 알게 되었다. `sample_data_to_object()`를 만들어 샘플 데이터를 객체 리스트로 변환하는 작업을 진행하고 있다. **(해결중)**

- **클래스(Class)와 객체(Object) / 인스턴스(Instance)의 개념을 다시 학습하였다.** 클래스는 데이터와 기능을 정의해 둔 설계도이며, `PaperBook(...)`, `EBook(...)`처럼 클래스에 실제 데이터를 전달해 생성한 각각의 도서가 객체(인스턴스)라는 것을 코드로 이해하였다. **(해결)**
  - `book_info`의 각 딕셔너리는 도서의 데이터이고, 이를 이용해 `PaperBook` / `EBook` 객체를 생성하면 `display_info()`, Getter/Setter 등 클래스에서 정의한 기능을 사용할 수 있다.
  - 생성한 여러 도서 객체는 `books` 리스트에 저장하여 조회, 검색, 대여·반납 기능에서 공통으로 사용할 예정이다.
  - 현재 시각 오전 1시 1분... 머리가 안 돌아간다.. 하기 싫은 건가... 일단 자고 내일 끝내자!!


### Date: 2026-08-24 (final deadline - 제출 못함)

- Windows 환경에서 `git pull origin main` + `uv sync` 성공
- 새로 정리한 개념 (**자세한 TIL은 ML의 `20260824` 파일에 확인**)
  - `dict.items()` → key와 value를 함께 순회
  - `return A, B` → 여러 값 반환, 호출부에서 unpacking 가능
  - `Class` → 설계도
  - `Object / Instance` → 클래스로 실제 생성된 값
  - `book.display_info()` → 객체의 메서드 호출
  - `book.get_isbn()` → 객체의 private 데이터에 Getter로 접근
  - `isbn_set` → ISBN 존재 여부 / 중복 확인
  - `books` → 실제 `PaperBook`, `EBook` 객체 저장
  - `return` → 함수 종료
  - `print` → 출력만 수행


### Date: 2026-08-26

- 샘플 데이터를 실제 `PaperBook`, `EBook` 객체로 변환하는 과정에서 여러 오류가 발생했다.

#### 1. 샘플 데이터 → 객체 변환

**문제**
- `book_info.keys`를 사용하면서 `TypeError: 'builtin_function_or_method' object is not iterable` 발생
- `PaperBook` 객체 생성 시 클래스에 정의된 매개변수와 다른 `ISBN`을 전달하여 `unexpected keyword argument 'ISBN'` 오류 발생

**수정**
```python
isbn_set = set(book_info.keys())

for isbn, data in book_info.items():
    ...
```

- `keys()`와 `items()`처럼 메서드를 실제로 실행할 때는 `()`가 필요하다는 것을 확인하였다.
- 생성자의 매개변수 이름도 실제 클래스 정의와 동일하게 `isbn`으로 수정하였다. **(해결)**


#### 2. `tuple`과 `list` 혼동

**문제**
- `sample_data_to_object()`에서 `books, isbn_set` 두 개를 반환했지만 하나의 변수로 받아 이후 `books.append()`에서 아래 오류가 발생했다.

```text
AttributeError: 'tuple' object has no attribute 'append'
```

**수정**
```python
books, isbn_set = sample_data_to_object()
```

- 반환값을 각각 unpacking하여 `books`는 객체를 저장하는 `list`, `isbn_set`은 ISBN을 관리하는 `set`으로 분리하였다. **(해결)**


#### 3. 클래스에서 직접 `display_info()` 호출

**문제**
```python
PaperBook.display_info()
```

위와 같이 호출하여 `self`가 없다는 오류가 발생했다.

**수정**
```python
for book in books:
    print(book.display_info())
```

- `books` 안에 들어 있는 실제 객체를 하나씩 꺼내 객체의 메서드를 호출하도록 변경하였다. **(해결)**
- 8월 24일에 정리했던 Class / Object 개념을 실제 코드에서 다시 적용해볼 수 있었다.


#### 4. `display_info()` 출력 형식 및 들여쓰기 깨짐

도서 정보를 여러 줄로 출력하는 과정에서 코드의 들여쓰기, 실제 줄바꿈, `\n`이 섞이면서 불필요한 공백과 빈 줄이 출력되는 문제가 있었다.

**1차 시도 — 삼중 따옴표와 `\n` 사용**

```python
return f"""[전자도서] {base_info}
        형식: {self.__ftype}\n
        크기: {self.__size} MB\n
        """
```

- 여러 줄 문자열 자체의 줄바꿈과 코드상 들여쓰기까지 출력 문자열에 영향을 주었다.
- 여기에 `\n`까지 추가하면서 불필요한 빈 줄이 생겼다.

**2차 시도 — 여러 f-string 연결**

```python
return (
    f"[전자도서]\n"
    f"{base_info}"
    f"형식: {self.__ftype}\n"
    f"크기: {self.__size} MB\n"
)
```

- 코드의 들여쓰기와 실제 출력 형식을 분리하기 쉬워졌다.
- 다만 `base_info` 마지막에 `\n`이 있는지도 함께 확인해야 했다.

**3차 시도 — `%` 문자열 포맷팅**

```python
return "[일반도서]\n%s\n위치: %s 층\n" % (base_info, self.__location)
```

- `%s`를 이용한 문자열 포맷팅 방식도 적용해 보았다.

**배운 점**
- 출력이 깨지는 것은 단순히 f-string이나 `%` 포맷팅 방식의 차이 때문이 아니라, **문자열 자체의 줄바꿈과 `\n`, 공백의 위치**가 영향을 준다는 것을 확인하였다.
- 부모 클래스에서 받아온 `base_info`에 이미 줄바꿈이 있다면 자식 클래스에서 추가하는 줄바꿈과 중복될 수 있다. **(해결)**


### Date: 2026-08-27

#### 1. 숫자 입력 검증 후 `UnboundLocalError`

**문제**
- 도서 위치에 `1층`, 전자도서 용량에 `50.3mb`를 입력해보니 `ValueError`를 출력한 뒤 다시 `UnboundLocalError`가 발생했다.
- `except`로 오류 메시지는 출력했지만 변환에 실패하여 `num`이나 `info`가 생성되지 않은 상태에서 이후 코드가 계속 실행되고 있었다.

**수정**
```python
def get_valid_integer(prompt):
    while True:
        try:
            num = int(input(prompt))
            return num

        except ValueError as e:
            print(f"[경고]-{e}: 입력하신 값이 없거나 올바르지 않습니다.")
```

`get_float()`도 동일하게 수정하였다.

**테스트**
```text
전자 도서 파일의 용량을 입력하세요.: 50.3mb
[경고] ... 올바르지 않습니다.

전자 도서 파일의 용량을 입력하세요.: 50.5
전자 도서 등록이 완료되었습니다.
```

잘못 입력해도 프로그램이 종료되지 않고 다시 입력받는 것을 확인하였다. **(해결)**

**배운 점**
- `except`가 오류를 처리한다고 해서 그 아래 코드의 실행까지 자동으로 중단되는 것은 아니다.
- 입력 검증은 `while True → try → 변환 성공 → return / 실패 → except → 다시 입력` 구조로 만들 수 있다.


#### 2. `continue`, `else`, `ValueError` 사용법 재확인

대여/반납 기능을 구현하면서 메인 메뉴로 돌아가는 방법과 잘못된 입력을 처리하는 방법을 혼동하였다.

- `continue` → 현재 반복의 남은 부분을 건너뛰고 다음 반복으로 이동
- `else` → `elif`와 달리 별도의 조건 없이 사용
- `ValueError` → `else`에 작성하는 것이 아니라 `try / except`에서 처리

이 부분은 아침에 튜터님께 다시 질문하여 개념을 확인하였다. **(해결)**


#### 3. 도서 검색 기능 확장

기존에는 ISBN을 알고 있어야만 도서를 검색할 수 있었다.

```python
if book.get_isbn() == isbn:
    ...
```

하지만 실제 사용자는 ISBN을 모를 가능성이 있기 때문에 검색 기능에서는 ISBN뿐만 아니라 **도서명 또는 저자명에 검색어가 포함되어 있어도 결과가 나오도록** 구조를 변경하였다.

```python
for book in books:
    if book.get_isbn() == keyword:
        ...

    if keyword in book.get_title():
        ...

    if keyword in book.get_author():
        ...
```

- 대여/반납은 정확한 도서를 특정해야 하므로 ISBN을 사용
- 일반 검색은 ISBN / 도서명 / 저자명 검색을 지원하도록 역할을 구분하였다. **(해결)**


#### 4. 대여/반납 상태 처리

하나의 메뉴에서 대여와 반납을 어떻게 구분할지 고민하였다.

현재 객체의 대여 상태를 Getter로 확인한 뒤 Boolean 값에 따라 동작하도록 구성하였다.

```python
if book.get_status() is True:
    # 현재 대여 중 → 반납
    book.set_status(False)

else:
    # 현재 대여 가능 → 대여
    book.set_status(True)
```

- `True` → 대여 중
- `False` → 대여 가능

Getter로 현재 상태를 확인하고 Setter로 객체의 상태를 변경하는 흐름을 실제 기능에 적용하였다. **(해결)**


#### 5. CLI 출력 형식 정리

기능 구현 이후 실제 사용 화면을 조금 더 알아보기 쉽도록 입력, 안내, 완료, 경고 메시지의 형식을 정리하였다.

```text
📌 등록할 도서의 ISBN을 입력하세요.
→

ℹ️ [안내]

✅ [완료]

⚠️ [경고]
```

기능 구현뿐만 아니라 사용자가 현재 어떤 입력을 해야 하고 어떤 결과가 발생했는지 구분할 수 있도록 CLI 화면도 정리하였다.


#### 6. 전체 기능 통합 테스트

등록 → 전체 조회 → 검색 → 대여 → 반납 → 종료까지 하나의 실행 환경에서 순서대로 테스트하였다.

**정상 작동 확인**
- 신규 일반도서 등록
- 등록한 도서를 전체 조회에서 확인
- ISBN 검색
- 도서명 검색
- 도서 대여
- 대여 상태 변경 확인
- 도서 반납
- 프로그램 정상 종료

**통합 테스트에서 추가로 발견한 문제**

1. **대여/반납 메뉴에서 도서 정보가 중복 출력됨**
   - 메인 코드와 대여/반납 처리 과정에서 `display_info()`가 중복 호출되고 있는지 확인 필요

2. **대여 완료 직후 출력되는 도서 상태가 `[대여 가능]`으로 남아 있음**
   - 실제 객체의 상태 변경 자체는 성공하여 다음 조회에서는 `[대여 중]`으로 표시됨
   - 상태 변경 전에 `target_info = book.display_info()`로 만들어둔 문자열을 상태 변경 후에도 다시 출력한 것이 원인으로 추정
   - `set_status(True)` 이후 최신 `display_info()`를 다시 호출하도록 수정 필요

3. **전자도서 출력에서 일부 줄바꿈이 붙는 현상**
```text
상태: [대여 가능]형식: PDF
```
   - 부모 클래스의 `base_info` 마지막 `\n`과 자식 클래스의 출력 문자열 연결 부분을 다시 확인할 예정

**(수정 진행중)**


#### 추가 테스트 예정

- 기존 ISBN으로 중복 등록 시 정상적으로 차단되는지
- 신규 도서 등록 후 즉시 검색되는지
- 도서명의 일부 단어만 입력해도 검색되는지
- 저자명의 일부만 입력해도 검색되는지
- 존재하지 않는 ISBN / 제목 / 저자 검색
- 존재하지 않는 ISBN으로 대여/반납 시도
- 페이지 수에 `300쪽` 등 잘못된 입력
- 파일 용량에 `50MB` 등 잘못된 입력
- 대여 → 전체 조회 → 반납 → 전체 조회 시 상태값 정상 변경 확인


---

## Review

이번 과제에서는 처음부터 코드를 완성하기보다 **환경 구성 → 데이터 구조 → 클래스 → 객체 생성 → CLI → 기능 연결 → 예외처리 → 통합 테스트** 순서로 하나씩 구현하였다.

특히 개별적으로 배웠던 `dict`, `list`, `set`, Class/Object, Getter/Setter, `try/except`, 반복문이 실제 하나의 프로그램 안에서 어떻게 연결되는지 직접 확인할 수 있었다.

디버깅 과정에서는 단순히 오류가 발생한 줄만 수정하는 것보다 **현재 변수의 자료형이 무엇인지, Class인지 Object인지, 함수가 무엇을 return하는지, 예외 발생 후 코드가 어디로 이동하는지, 객체의 상태가 어느 시점에 변경되는지**를 순서대로 확인하는 것이 중요하다는 점을 배웠다.

또한 개별 기능이 정상 작동하더라도 `등록 → 조회 → 검색 → 대여 → 반납`처럼 실제 사용자 흐름대로 통합 테스트하면 중복 출력이나 상태 출력 시점처럼 새로운 문제를 발견할 수 있다는 점도 확인하였다.